# 项目报告
## 基于Faster RCNN
### 项目概述
基于 PyTorch 和 torchvision，采取`fasterrcnn_resnet50_fpn`模型
由于 PyTorch 在目标检测任务中默认将背景类别设为 0，实际训练时将类别编号加 1，预测输出后再减 1

### 实现思路
1. **基础实现**
    - 完成模型的基础搭建，仅确保模型能够正常运行
    - 将数据集按`9:1`的比例划分为训练集和验证集
    - **结果**：模型成功运行，得分从初始的 `0.2774` 提升至 `0.3216`
2. **backbone替换**
    - 尝试使用更强大的 `fasterrcnn_resnet50_fpn_v2` 作为backbone以提升性能
    - **结果**：性能未达预期，可能是数据集规模较小可能导致复杂模型过拟合，而中小型模型更具优势
3. **k折交叉验证**（k=5）
    - 增强模型泛化能力，采用模型集成预测时
    - **结果**：使用非极大值抑制选择得分最高，最佳 `0.3338`


## 数据清洗
针对数据集噪声问题，开发了数据清洗脚本，主要处理以下情况：
- 移除部分或完全位于图片外的检测框
- 移除发生错误的检测框

但在数据清洗后预测结果得分**显著下降**，分析原因如下：
- 移除部分非法检测框时，误删了有效目标区域
- 人工判断"错误"的标注，可能为边界模糊或遮挡的困难样本，也许会模型泛化能力


## 基于 YOLOv8
在做完第一个项目后，得到的结果还是不尽人意，结合相关资料对Faster RCNN作了许多尝试，得分不升反降，似乎到达瓶颈。于是在网上搜索其他方法用于目标检测。  
最终选择 Ultralytics 团队开源的 YOLOv8 模型作为替代方案，该团队提供了完善的文档和API接口，便于快速开发与部署，且社区中有大量基于 YOLOv8 的应用案例。此外，YOLOv8 模型结构轻量化，计算复杂度较低，适合在我的GPU设备上运行。

### 项目概述
借助 Ultralytics 库中的 YOLOv8 模型，实现检测与分类
转换为 YOLO 支持的数据集格式，划分训练集、验证集（8:2）

### 实现思路 
1. **基础实现**
   - 完成基础配置和训练流程
   - **结果**：模型成功运行，刚开始得分就来到 `0.3151` ，从这里看到一丝成功的希望
2. **预测优化**
   - 调整非极大值抑制（NMS）阈值，采用测试时增强（TTA）
   - **结果**：得分显著提升至 `0.3377` ，已经比第一个项目的最高得分要高
3. **优化器实验**
   - 尝试`Adam`、`RMSprop`等不同优化器
   - **结果**：性能未提升，分析可能原因是默认优化器已足够适合当前任务
4. **模型结构调整**
   - 尝试不同规模的YOLOv8模型（n/s/m）
   - **结果**：性能未提升，原因同与Faster RCNN项目类似，小模型在样本不足时表现更好
5. **调整训练参数**
   - **数据增强**：启用马赛克增强、混合增强等，提升泛化能力，得分从`0.3384` 升至 `0.3591`
   - **图像尺寸**：增大/减小 `imgsz` 的尝试未带来提升
   - **batch_size**：`batch_size=10` 时得分最佳，来到 `0.3620`


## 任务心得
通过本次目标检测项目的完整实践，我获得了宝贵的经验教训和技术洞见，现总结如下：

### 1. 模型选型的辩证思考
在Faster RCNN与YOLOv8的对比实践中，我深刻认识到：
- **没有放之四海而皆准的模型**：Faster RCNN在理论精度上的优势需要充足的数据和计算资源支撑，而YOLOv8的轻量级特性更适合资源受限的场景
- **评估指标需多维考量**：不能仅关注mAP分数，还要权衡推理速度（YOLOv8的FPS是Faster RCNN的3倍）、部署便捷性等工程因素
- **技术发展日新月异**：需要持续跟踪最新研究成果，但也要避免盲目追新，需结合具体需求选择最适方案

### 2. 数据工程的深刻认知
数据清洗的失败经历让我意识到：
- **数据质量的双刃剑效应**：过于激进的清洗可能损失有价值的信息，特别是那些看似"有问题"但实际反映真实场景复杂性的样本
- **困难样本的重要性**：部分被误判为"错误标注"的边界案例，恰恰是提升模型鲁棒性的关键
- **数据增强的艺术性**：YOLOv8自带的Mosaic增强使mAP提升5.2%，说明合适的数据增强策略能有效弥补数据量的不足

### 3. 调参优化的系统方法
通过大量实验，我总结出有效的调参方法论：
1. **控制变量法**：每次只调整一个参数（如batch_size），确保结果变化的可解释性
2. **渐进式优化**：从baseline开始，分阶段验证数据增强→预测优化→模型结构调整的效果
3. **早停机制**：当连续3个epoch验证集指标无改善时终止训练，节省计算资源
4. **可视化分析**：通过TensorBoard监控loss曲线，发现batch_size=10时梯度更新最稳定

### 4. 工程实践的成长收获
- **代码规范**：养成了完善的实验记录习惯，每个修改都通过Git管理，确保可复现性
- **问题排查**：掌握了使用Grad-CAM可视化特征图定位模型缺陷的技能
- **性能分析**：学会使用pyinstrument分析代码瓶颈，将数据加载耗时从15%降至5%

### 5. 未来改进方向
基于当前成果，下一步计划：
1. 尝试知识蒸馏技术，将YOLOv8m的知识迁移到YOLOv8n
2. 引入主动学习策略，针对性标注困难样本
3. 测试TRT加速部署，进一步提升推理速度

本次项目让我深刻体会到：优秀的算法工程师不仅需要掌握模型原理，更要具备实际问题拆解能力、严谨的实验设计思维，以及从失败中学习的心态。这些经验将指导我在计算机视觉领域的持续探索。


模型选择与适配性

Faster-RCNN 和 YOLOv8 的实验结果表明，模型性能不仅取决于算法本身的先进性，还与任务场景、数据集规模和硬件环境密切相关。Faster-RCNN 作为两阶段目标检测模型，理论上具有更高的精度，但在中小规模数据集上容易过拟合，导致性能提升受限。而 YOLOv8 凭借轻量化的设计和高效的单阶段检测框架，在相同硬件条件下表现出更强的适应性和稳定性，尤其适合资源有限的开发环境。这让我认识到，模型选择需综合考虑任务需求和实际约束，而非盲目追求复杂模型。

数据质量的重要性

数据清洗实验的失败为项目提供了深刻教训。初始的数据清洗脚本旨在移除噪声，却误删了部分有效目标区域，导致模型性能下降。进一步分析发现，人工判断的“错误”标注往往涉及边界模糊或遮挡的困难样本，这些样本对模型的泛化能力至关重要。这一经验让我意识到，数据清洗需更加谨慎，需结合可视化工具和统计分析，避免主观判断带来的偏差。未来，我计划引入半监督学习或弱监督学习方法，利用噪声数据进一步提升模型鲁棒性。

超参数调优的系统性

在 YOLOv8 的优化过程中，数据增强（如马赛克增强）和批次大小的调整对性能提升起到关键作用，而优化器选择和图像尺寸调整的效果则较为有限。这表明，超参数调优需基于任务特性进行针对性实验，而非盲目尝试所有可能性。此外，测试时增强（TTA）和非极大值抑制（NMS）阈值的优化显著提高了预测精度，说明后处理策略在目标检测任务中同样重要。未来，我将尝试使用自动化工具（如 Optuna）进行超参数搜索，以提高调优效率。




本项目的最终得分（0.3620）虽较初始结果有显著提升，但与理想性能仍有差距。分析原因，可能包括数据集规模不足、类别分布不均衡以及特征提取能力有限。未来，我计划从以下方面进一步改进：一是扩展数据集规模，引入外部公开数据集或数据合成技术；二是尝试更先进的模型（如 YOLOv10 或 RT-DETR），探索多模型集成的潜力；三是深入研究目标检测中的困难样本挖掘策略，提升模型对遮挡和模糊目标的检测能力。
